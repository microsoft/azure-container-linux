[Service]
# OverlayFS rechecks lower-layer access using the credentials that created the
# mount. The init_t domain cannot read or execute every service-specific type
# exposed through /usr, so use the policy's unconfined service domain for this
# privileged, short-lived mount helper.
SELinuxContext=system_u:system_r:initrc_t:s0
