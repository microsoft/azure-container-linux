# Provide the base config on images where no provisioning agent owns it.
# Existing files, including AKS-generated configuration, take precedence.
L /etc/containerd/config.toml - - - - /usr/share/containerd/config.toml
